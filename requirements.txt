# CLI: stdlib only (generate_activity_commits.py).

# Web UI (pip install -r requirements.txt)
fastapi>=0.109
uvicorn[standard]>=0.27
httpx>=0.26
jinja2>=3.1
python-multipart
itsdangerous
python-dotenv>=1.0
